---
name: Bug report
about: Something in the SDK behaves incorrectly
labels: bug
---

**Describe the bug**
What happened, and what did you expect instead?

**Reproduction**
```python
# Minimal snippet (redact credentials — never paste client_id/api_key/tokens)
```

**Environment**
- `airwallex` version:
- Python version:
- Airwallex environment: demo / production
- `api_version` pinned (if any):

**Error output**
Include the full exception (the SDK redacts tokens from errors, but double-check
before pasting) and the `request_id` from the error if present.
